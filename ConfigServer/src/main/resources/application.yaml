server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/GetSetAbhi/MICROSERVICES-SPRINGBOOT-DOCKER-KUBERNETES
          clone-on-start: true